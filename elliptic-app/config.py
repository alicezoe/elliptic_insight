# The config.py file is used to store configuration variables and settings.
# This can include file paths, constants, API keys (if not sensitive)
# model parameters, or any other settings you want to centralize.

# Data paths

# Model parameters
